#include <cstring>
#include <iostream>

int* pi(char* needle)
{
	int* ret = new int(strlen(needle));
	
	/*
	int j = 0;
	for (int i = 1; i < strlen(needle); ++i)
	{
		int k = ret[i-1]
		while 
		if(needle[i] != needle[j])
			j = 0;
		else
			++j;
		ret[i] = j;
	}*/
	return ret;
}